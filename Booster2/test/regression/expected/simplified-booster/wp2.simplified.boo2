system Test 
 


	class A  { 		attributes			n  : INT			m  : INT		invariants 			this.n > this.m		methods 			update { ((m? : INT & n? > m?) & n? : INT) --> ( (this.m := m?) || (this.n := n?) ) }	}